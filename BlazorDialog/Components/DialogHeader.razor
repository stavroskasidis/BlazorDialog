@namespace BlazorDialog
<div class="blazor-dialog-header @CssClass" >
    <div class="blazor-dialog-title">@ChildContent</div>
    @if (ShowClose)
    {
        <button @onclick="OnCloseClick" type="button" class="blazor-dialog-close" aria-label="Close" >
            <span aria-hidden="true">×</span>
        </button>
    }
</div>

@code{
    [Parameter] public RenderFragment ChildContent { get; set; } = null!;
    [Parameter] public bool ShowClose { get; set; } = false;
    [Parameter] public string? CssClass { get; set; }
    [CascadingParameter(Name = "ParentDialog")] Dialog Dialog { get; set; } = null!;

    private async Task OnCloseClick()
    {
        await Dialog.HideFromCloseButton();
    }
}